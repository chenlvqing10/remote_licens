#
# Input core configuration
#
menuconfig INPUT_IR
	bool "irs"
	default y
	help
	  Say Y here, and a list of supported ir will be displayed.
	  This option doesn't affect the kernel.

	  If unsure, say Y.

if INPUT_IR

config IR_LOMBO_N7
	tristate "Lombo ir"
	depends on ARCH_LOMBO_N7V1
	help
	  Say Y to enable infrared radiation module support for the Lombo Tech N7 platforms.

	  To compile this driver as a module, choose M here: the
	  module will be called lombo_ir.

endif
