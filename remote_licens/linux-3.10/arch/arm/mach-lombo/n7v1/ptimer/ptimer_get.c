/* ptimer_get.c */

#ifndef ___PTIMER__GET___C___
#define ___PTIMER__GET___C___

#ifdef DEF_GET_PTIMER_PTMR_VER_VER_L
fun_get_mod_reg_bit(ptimer, ptmr_ver, ver_l, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR_VER_VER_H
fun_get_mod_reg_bit(ptimer, ptmr_ver, ver_h, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR_VER_COMP
fun_get_mod_reg_bit(ptimer, ptmr_ver, comp, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR0_STORE_LOW_VAL_LOW
fun_get_mod_reg_bit(ptimer, ptmr0_store_low_val, low, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR0_STORE_HIGH_VAL_HIGH
fun_get_mod_reg_bit(ptimer, ptmr0_store_high_val, high, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR0_CUR_LOW_VAL_LOW
fun_get_mod_reg_bit(ptimer, ptmr0_cur_low_val, low, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR0_CUR_HIGH_VAL_HIGH
fun_get_mod_reg_bit(ptimer, ptmr0_cur_high_val, high, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR0_CTRL_ENABLE
fun_get_mod_reg_bit(ptimer, ptmr0_ctrl, enable, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR0_CTRL_MODE
fun_get_mod_reg_bit(ptimer, ptmr0_ctrl, mode, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR0_CTRL_DIRECTION
fun_get_mod_reg_bit(ptimer, ptmr0_ctrl, direction, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR0_CTRL_DIV
fun_get_mod_reg_bit(ptimer, ptmr0_ctrl, div, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR0_INT_EN_ENABLE
fun_get_mod_reg_bit(ptimer, ptmr0_int_en, enable, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR0_CLR_INT_PENDING_CLR
fun_get_mod_reg_bit(ptimer, ptmr0_clr_int_pending, clr, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR0_INT_PENDING_PENDING
fun_get_mod_reg_bit(ptimer, ptmr0_int_pending, pending, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR1_STORE_LOW_VAL_LOW
fun_get_mod_reg_bit(ptimer, ptmr1_store_low_val, low, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR1_STORE_HIGH_VAL_HIGH
fun_get_mod_reg_bit(ptimer, ptmr1_store_high_val, high, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR1_CUR_LOW_VAL_LOW
fun_get_mod_reg_bit(ptimer, ptmr1_cur_low_val, low, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR1_CUR_HIGH_VAL_HIGH
fun_get_mod_reg_bit(ptimer, ptmr1_cur_high_val, high, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR1_CTRL_ENABLE
fun_get_mod_reg_bit(ptimer, ptmr1_ctrl, enable, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR1_CTRL_MODE
fun_get_mod_reg_bit(ptimer, ptmr1_ctrl, mode, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR1_CTRL_DIRECTION
fun_get_mod_reg_bit(ptimer, ptmr1_ctrl, direction, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR1_CTRL_DIV
fun_get_mod_reg_bit(ptimer, ptmr1_ctrl, div, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR1_INT_EN_ENABLE
fun_get_mod_reg_bit(ptimer, ptmr1_int_en, enable, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR1_CLR_INT_PENDING_CLR
fun_get_mod_reg_bit(ptimer, ptmr1_clr_int_pending, clr, addr)
#endif

#ifdef DEF_GET_PTIMER_PTMR1_INT_PENDING_PENDING
fun_get_mod_reg_bit(ptimer, ptmr1_int_pending, pending, addr)
#endif

#endif /* ___PTIMER__GET___C___ */
