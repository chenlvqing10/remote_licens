/* gpadc_get.h */

#ifndef ___GPADC__GET___H___
#define ___GPADC__GET___H___

#ifdef DEF_GET_GPADC_VER_VER_L
def_get_mod_reg_bit(gpadc, ver, ver_l, addr);
#endif

#ifdef DEF_GET_GPADC_VER_VER_H
def_get_mod_reg_bit(gpadc, ver, ver_h, addr);
#endif

#ifdef DEF_GET_GPADC_VER_COMP
def_get_mod_reg_bit(gpadc, ver, comp, addr);
#endif

#ifdef DEF_GET_GPADC_GPAEN_GEN
def_get_mod_reg_bit(gpadc, gpaen, gen, addr);
#endif

#ifdef DEF_GET_GPADC_GPAEN_CEN
def_get_mod_reg_bit(gpadc, gpaen, cen, addr);
#endif

#ifdef DEF_GET_GPADC_GPAC_AUXIN0E
def_get_mod_reg_bit(gpadc, gpac, auxin0e, addr);
#endif

#ifdef DEF_GET_GPADC_GPAC_AUXIN1E
def_get_mod_reg_bit(gpadc, gpac, auxin1e, addr);
#endif

#ifdef DEF_GET_GPADC_GPAC_SDAE
def_get_mod_reg_bit(gpadc, gpac, sdae, addr);
#endif

#ifdef DEF_GET_GPADC_GPAC_SRC
def_get_mod_reg_bit(gpadc, gpac, src, addr);
#endif

#ifdef DEF_GET_GPADC_GPAC_FSS
def_get_mod_reg_bit(gpadc, gpac, fss, addr);
#endif

#ifdef DEF_GET_GPADC_GPAC_CCE
def_get_mod_reg_bit(gpadc, gpac, cce, addr);
#endif

#ifdef DEF_GET_GPADC_GPAC_DIVE
def_get_mod_reg_bit(gpadc, gpac, dive, addr);
#endif

#ifdef DEF_GET_GPADC_GPAC_OPC
def_get_mod_reg_bit(gpadc, gpac, opc, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIE_AUX0IE
def_get_mod_reg_bit(gpadc, gpaie, aux0ie, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIE_AUX1IE
def_get_mod_reg_bit(gpadc, gpaie, aux1ie, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIE_S0DIE
def_get_mod_reg_bit(gpadc, gpaie, s0die, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIE_S0UIE
def_get_mod_reg_bit(gpadc, gpaie, s0uie, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIE_S1DIE
def_get_mod_reg_bit(gpadc, gpaie, s1die, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIE_S1UIE
def_get_mod_reg_bit(gpadc, gpaie, s1uie, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIE_S2DIE
def_get_mod_reg_bit(gpadc, gpaie, s2die, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIE_S2UIE
def_get_mod_reg_bit(gpadc, gpaie, s2uie, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIE_AIN0DATIE
def_get_mod_reg_bit(gpadc, gpaie, ain0datie, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIE_AIN1DATIE
def_get_mod_reg_bit(gpadc, gpaie, ain1datie, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIE_S0DATIE
def_get_mod_reg_bit(gpadc, gpaie, s0datie, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIE_S1DATIE
def_get_mod_reg_bit(gpadc, gpaie, s1datie, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIE_S2DATIE
def_get_mod_reg_bit(gpadc, gpaie, s2datie, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIS_AUX0IS
def_get_mod_reg_bit(gpadc, gpais, aux0is, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIS_AUX1IS
def_get_mod_reg_bit(gpadc, gpais, aux1is, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIS_S0DIS
def_get_mod_reg_bit(gpadc, gpais, s0dis, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIS_S0UIS
def_get_mod_reg_bit(gpadc, gpais, s0uis, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIS_S1DIS
def_get_mod_reg_bit(gpadc, gpais, s1dis, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIS_S1UIS
def_get_mod_reg_bit(gpadc, gpais, s1uis, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIS_S2DIS
def_get_mod_reg_bit(gpadc, gpais, s2dis, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIS_S2UIS
def_get_mod_reg_bit(gpadc, gpais, s2uis, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIS_AIN0DATIS
def_get_mod_reg_bit(gpadc, gpais, ain0datis, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIS_AIN1DATIS
def_get_mod_reg_bit(gpadc, gpais, ain1datis, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIS_S0DATIS
def_get_mod_reg_bit(gpadc, gpais, s0datis, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIS_S1DATIS
def_get_mod_reg_bit(gpadc, gpais, s1datis, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIS_S2DATIS
def_get_mod_reg_bit(gpadc, gpais, s2datis, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIC_AUXIN0PC
def_get_mod_reg_bit(gpadc, gpaic, auxin0pc, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIC_AUXIN1PC
def_get_mod_reg_bit(gpadc, gpaic, auxin1pc, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIC_S0DPC
def_get_mod_reg_bit(gpadc, gpaic, s0dpc, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIC_S0UPC
def_get_mod_reg_bit(gpadc, gpaic, s0upc, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIC_S1DPC
def_get_mod_reg_bit(gpadc, gpaic, s1dpc, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIC_S1UPC
def_get_mod_reg_bit(gpadc, gpaic, s1upc, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIC_S2DPC
def_get_mod_reg_bit(gpadc, gpaic, s2dpc, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIC_S2UPC
def_get_mod_reg_bit(gpadc, gpaic, s2upc, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIC_AIN0DATPC
def_get_mod_reg_bit(gpadc, gpaic, ain0datpc, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIC_AIN1DATPC
def_get_mod_reg_bit(gpadc, gpaic, ain1datpc, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIC_S0DATPC
def_get_mod_reg_bit(gpadc, gpaic, s0datpc, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIC_S1DATPC
def_get_mod_reg_bit(gpadc, gpaic, s1datpc, addr);
#endif

#ifdef DEF_GET_GPADC_GPAIC_S2DATPC
def_get_mod_reg_bit(gpadc, gpaic, s2datpc, addr);
#endif

#ifdef DEF_GET_GPADC_CDAT_DAT
def_get_mod_reg_bit(gpadc, cdat, dat, addr);
#endif

#ifdef DEF_GET_GPADC_AIN0DAT_DAT
def_get_mod_reg_bit(gpadc, ain0dat, dat, addr);
#endif

#ifdef DEF_GET_GPADC_AUX0HDAT_AUXIN0SHDAT
def_get_mod_reg_bit(gpadc, aux0hdat, auxin0shdat, addr);
#endif

#ifdef DEF_GET_GPADC_AUX0TDAT_AUXIN0TDAT
def_get_mod_reg_bit(gpadc, aux0tdat, auxin0tdat, addr);
#endif

#ifdef DEF_GET_GPADC_AIN1DAT_DAT
def_get_mod_reg_bit(gpadc, ain1dat, dat, addr);
#endif

#ifdef DEF_GET_GPADC_AUX1HDAT_AUXIN1HDAT
def_get_mod_reg_bit(gpadc, aux1hdat, auxin1hdat, addr);
#endif

#ifdef DEF_GET_GPADC_AUX1TDAT_AUXIN1TDAT
def_get_mod_reg_bit(gpadc, aux1tdat, auxin1tdat, addr);
#endif

#ifdef DEF_GET_GPADC_GSC_SEN
def_get_mod_reg_bit(gpadc, gsc, sen, addr);
#endif

#ifdef DEF_GET_GPADC_GSC_SSDAEN
def_get_mod_reg_bit(gpadc, gsc, ssdaen, addr);
#endif

#ifdef DEF_GET_GPADC_GSC_SSR
def_get_mod_reg_bit(gpadc, gsc, ssr, addr);
#endif

#ifdef DEF_GET_GPADC_S0CDAT_S0CDAT
def_get_mod_reg_bit(gpadc, s0cdat, s0cdat, addr);
#endif

#ifdef DEF_GET_GPADC_S1CDAT_S1CDAT
def_get_mod_reg_bit(gpadc, s1cdat, s1cdat, addr);
#endif

#ifdef DEF_GET_GPADC_S2CDAT_S2CDAT
def_get_mod_reg_bit(gpadc, s2cdat, s2cdat, addr);
#endif

#ifdef DEF_GET_GPADC_S0DAT_S0ADCDAT
def_get_mod_reg_bit(gpadc, s0dat, s0adcdat, addr);
#endif

#ifdef DEF_GET_GPADC_S0UTDAT_S0DATUT
def_get_mod_reg_bit(gpadc, s0utdat, s0datut, addr);
#endif

#ifdef DEF_GET_GPADC_S0DTDAT_S0DATDT
def_get_mod_reg_bit(gpadc, s0dtdat, s0datdt, addr);
#endif

#ifdef DEF_GET_GPADC_S1DAT_S1ADCDAT
def_get_mod_reg_bit(gpadc, s1dat, s1adcdat, addr);
#endif

#ifdef DEF_GET_GPADC_S1UTDAT_S1DATUT
def_get_mod_reg_bit(gpadc, s1utdat, s1datut, addr);
#endif

#ifdef DEF_GET_GPADC_S1DTDAT_S1DATDT
def_get_mod_reg_bit(gpadc, s1dtdat, s1datdt, addr);
#endif

#ifdef DEF_GET_GPADC_S2DAT_S2ADCDAT
def_get_mod_reg_bit(gpadc, s2dat, s2adcdat, addr);
#endif

#ifdef DEF_GET_GPADC_S2UTDAT_S2DATUT
def_get_mod_reg_bit(gpadc, s2utdat, s2datut, addr);
#endif

#ifdef DEF_GET_GPADC_S2DTDAT_S2DATDT
def_get_mod_reg_bit(gpadc, s2dtdat, s2datdt, addr);
#endif

#endif /* ___GPADC__GET___H___ */
